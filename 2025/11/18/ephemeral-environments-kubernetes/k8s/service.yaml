apiVersion: v1
kind: Service
metadata:
  name: myapp
  namespace: ${NAMESPACE}
  labels:
    app: myapp
    env: pr-${PR_NUMBER}
spec:
  selector:
    app: myapp
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP

