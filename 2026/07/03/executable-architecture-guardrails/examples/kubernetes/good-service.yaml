apiVersion: v1
kind: Service
metadata:
  name: payment-api
  namespace: production
  annotations:
    auth REQUIRED: "true"
    owner: payments-team
    data-classification: confidential
spec:
  type: LoadBalancer
  selector:
    app: payment-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
